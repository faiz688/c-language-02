#include <stdio.h>

int main() {
    int a, b, c, d; // you can declare multiple variables of the same type in a single line
    // variable declaration can be done in multiple lines as well
    return 0;
}
