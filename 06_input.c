#include <stdio.h>

int main() {
    int a; 
    scanf("%d", &a); // taking input from user and storing it in variable 'a'
    printf("You entered: %d\n", a); // printing the value of 'a' to the console
   
    return 0;
}